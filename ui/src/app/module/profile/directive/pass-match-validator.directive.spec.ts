import { PassMatchValidatorDirective } from './pass-match-validator.directive';

describe('MatchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PassMatchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
