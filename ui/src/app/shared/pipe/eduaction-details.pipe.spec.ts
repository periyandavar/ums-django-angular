import { EduactionDetailsPipe } from './eduaction-details.pipe';

describe('EduactionDetailsPipe', () => {
  // it('create an instance', () => {
  //   const pipe = new EduactionDetailsPipe();
  //   expect(pipe).toBeTruthy();
  // });
});
